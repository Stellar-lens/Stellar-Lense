# data/pipelines

Raw + processed Stellar DEX trade data, labelled training sets, and the
ingestion pipelines that produce them. Migrates from `Ledgerlens-data`.

Not a package in either workspace (JS or Python) — data and pipeline
scripts, consumed by `packages/core`. See
`docs/stellar-lens-project-plan.md` §2.

Not yet migrated — this is skeleton only.
