global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Scrape every scorer replica's /metrics endpoint. docker-compose service DNS
  # resolves "stellar-lense-scorer" to all replica IPs via dns_sd.
  - job_name: stellar-lense-scorer
    dns_sd_configs:
      - names:
          - stellar-lense-scorer
        type: A
        port: 9100
