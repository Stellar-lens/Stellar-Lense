# apps/api

Stellar Lense public REST API. Migrates from `Ledegerlens-api` (FastAPI).

Managed as a Python workspace member (root `pyproject.toml`, `uv`). See
`docs/stellar-lens-project-plan.md` §2 for the workspace tooling plan and §3
for the rebrand checklist (env var prefixes, response headers, etc.) to apply
once content migrates here.

Not yet migrated — this is skeleton only.
