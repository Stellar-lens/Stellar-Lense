{
  "name": "@ledgerlens/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the LedgerLens API with Zod runtime validation",
  "keywords": [
    "ledgerlens",
    "stellar",
    "risk-scoring",
    "compliance",
    "wash-trading"
  ],
  "homepage": "https://github.com/Ledger-Lenz/Ledgerlens-core#readme",
  "license": "MIT",
  "author": "LedgerLens",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/types/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/types/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build:esm && npm run build:cjs && npm run build:types",
    "build:esm": "tsc --project tsconfig.esm.json",
    "build:cjs": "tsc --project tsconfig.cjs.json",
    "build:types": "tsc --project tsconfig.types.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "//lint": "TODO(#774): add a real `lint` script invoking ESLint once the eslint/prettier config lands (https://github.com/Ledger-Lenz/Ledgerlens-core/issues/774). Until then use `npm run typecheck` for type-only checks.",
    "prepublishOnly": "npm run build && npm run test"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
