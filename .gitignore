__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

.env
*.db

models/*.pkl
models/*.joblib
models/archive/
models/optuna_studies.db
models/best_params_*.json
reports/
# Keep sample/committed reports but ignore generated load test output
reports/load_test_results.json

# mutmut mutation testing cache
.mutmut-cache

# Pipeline checkpoint/resume state (utils/checkpoint.py) — run-local progress
# files and cached per-unit artifacts, never committed.
.checkpoints/

.DS_Store
