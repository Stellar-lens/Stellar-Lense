# packages/ui

Shared design system / component library for `apps/web`. New package.

Implements the tokens in `docs/DESIGN_SYSTEM.md`. pnpm
workspace member.

Not yet implemented — this is skeleton only.
