[project]
name = "stellar-lense-sdk"
version = "0.0.0"
description = "Stellar Lense Python SDK"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
