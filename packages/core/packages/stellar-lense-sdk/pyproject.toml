[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stellar-lense-sdk"
version = "0.1.0"
description = "Typed Python client for the Stellar Lense wash-trading detection API"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.6.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[project.urls]
Homepage = "https://github.com/Stellar-lens/Stellar-Lense"
Repository = "https://github.com/Stellar-lens/Stellar-Lense"

[tool.hatch.build.targets.wheel]
packages = ["src/stellar_lense"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
