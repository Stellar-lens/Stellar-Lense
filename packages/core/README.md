# packages/core

Stellar Lense detection engine. Migrates from `Ledgerlens-core`
(ingestion/, detection/, config/).

Managed as a Python workspace member (root `pyproject.toml`, `uv`). Note the
source repo has 1,000+ commits — the project plan recommends `git subtree`
or `git filter-repo` to preserve history when migrating, not a fresh
copy-paste. See `docs/stellar-lens-project-plan.md` §2 and §6.

Not yet migrated — this is skeleton only.
