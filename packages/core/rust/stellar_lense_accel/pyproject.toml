[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "stellar-lense-accel"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "stellar_lense_accel"
features = ["pyo3/extension-module"]
