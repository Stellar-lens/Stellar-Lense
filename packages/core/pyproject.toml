[project]
name = "stellar-lense-core"
version = "0.0.0"
description = "Stellar Lense detection engine: ingestion, Benford's Law analysis, ML ensemble, SHAP explanations"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
