# packages/sdk-ts

Stellar Lense TypeScript SDK. Migrates from `core/sdk` in the old LedgerLens core repo.

pnpm workspace member, consumed by `apps/web` and `apps/bot`.

Not yet migrated — this is skeleton only.
