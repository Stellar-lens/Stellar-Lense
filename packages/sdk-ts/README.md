# packages/sdk-ts

Stellar Lense TypeScript SDK. Migrated from `core/sdk` in this project's original, pre-monorepo core repo.

pnpm workspace member, consumed by `apps/web` and `apps/bot`.

Not yet migrated — this is skeleton only.
