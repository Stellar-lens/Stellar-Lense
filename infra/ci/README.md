# infra/ci

Consolidated GitHub Actions workflows, migrated from the org-wide `.github`
repo's CI/CD patterns. See `docs/stellar-lens-project-plan.md` §2 ("skip
`.github`'s content, fold its CI patterns in") and §6 ("get the existing CI
green in the new structure before touching any feature work").

Not yet populated — this is skeleton only.
