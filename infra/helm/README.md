# infra/helm

Helm charts for deploying `apps/api`, `apps/web`, `apps/bot`, and supporting
services. New — consolidates deployment config previously scattered across
the source repos.

Not yet populated — this is skeleton only.
